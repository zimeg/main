# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Deploy an app
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy to Slack
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Deploy the app
        uses: zimeg/slack-github-action/cli@v2.2.0-rc.3
        with:
          command: "deploy --app ${{ vars.SLACK_APP_ID }} --force"
          token: ${{ secrets.SLACK_SERVICE_TOKEN }}

      - name: Post a deployment message
        uses: zimeg/slack-github-action@v2.2.0-rc.3
        with:
          errors: true
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            channel: ${{ secrets.SLACK_CHANNEL_ID }}
            text: "Deployed to Slack from ${{ github.sha }}"
